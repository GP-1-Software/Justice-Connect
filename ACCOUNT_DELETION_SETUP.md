# 🔐 نظام طلبات حذف الحساب

## ✅ ما تم إضافته:

### **1. جدول طلبات الحذف**
جدول `account_deletion_requests` في قاعدة البيانات لتتبع طلبات حذف الحسابات.

### **2. API Service**
`src/services/deletionRequestApi.js` - دوال للتعامل مع طلبات الحذف.

### **3. واجهة المستخدم**
- ✅ نموذج طلب حذف مع سبب
- ✅ عرض حالة الطلب (قيد المراجعة، موافق، مرفوض)
- ✅ Modal جميل لطلب الحذف
- ✅ دعم Dark Mode

---

## 📋 الخطوات المطلوبة:

### **الخطوة 1: شغل SQL في Supabase**

اذهب إلى **Supabase Dashboard** → **SQL Editor** وشغل:

```sql
-- انسخ محتوى account_deletion_requests_table.sql والصقه هنا
```

هذا سينشئ:
- ✅ جدول `account_deletion_requests`
- ✅ الـ indexes المطلوبة
- ✅ الـ triggers لتحديث التواريخ

---

## 🧪 كيفية الاستخدام:

### **للعميل:**

1. **اذهب إلى الملف الشخصي** → **إدارة الحساب**
2. **اضغط "طلب حذف الحساب"**
3. **اكتب سبب الطلب** (مطلوب)
4. **اضغط "إرسال الطلب"**
5. **سيظهر إشعار:** "تم إرسال طلب حذف الحساب بنجاح"

### **حالات الطلب:**

#### **🟡 قيد المراجعة:**
- يظهر إشعار أصفر
- الزر يصبح غير مفعّل
- الرسالة: "سيتم مراجعة طلبك من قبل الإدارة قريباً"

#### **🟢 موافق عليه:**
- يظهر إشعار أحمر
- الرسالة: "سيتم حذف حسابك قريباً"
- الحساب سيتم حذفه تلقائياً

#### **🔴 مرفوض:**
- يظهر إشعار أخضر
- الرسالة: "تم رفض طلب حذف الحساب"
- يظهر سبب الرفض من الإدارة

---

## 👨‍💼 للـ Admin (لاحقاً):

ستحتاج لإنشاء صفحة Admin Dashboard لإدارة طلبات الحذف:

### **المطلوب:**
1. صفحة لعرض جميع طلبات الحذف المعلقة
2. إمكانية الموافقة أو الرفض
3. إضافة ملاحظات من الإدارة

### **الـ API جاهز:**
```javascript
// جلب جميع الطلبات المعلقة
getPendingDeletionRequests()

// تحديث حالة الطلب
updateDeletionRequestStatus(requestId, status, adminId, adminNotes)
```

---

## 🔒 الأمان:

- ✅ العميل لا يستطيع حذف حسابه مباشرة
- ✅ كل طلب يجب مراجعته من الإدارة
- ✅ العميل يرى حالة طلبه فقط
- ✅ الحساب يُحذف فقط بعد موافقة الإدارة

---

## 📊 قاعدة البيانات:

### **جدول `account_deletion_requests`:**

| العمود | النوع | الوصف |
|--------|-------|-------|
| `request_id` | SERIAL | معرف الطلب (Primary Key) |
| `user_id` | INTEGER | معرف المستخدم |
| `user_type` | VARCHAR | نوع المستخدم (client/lawyer) |
| `reason` | TEXT | سبب طلب الحذف |
| `status` | VARCHAR | حالة الطلب (pending/approved/rejected) |
| `requested_at` | TIMESTAMPTZ | تاريخ الطلب |
| `reviewed_by` | INTEGER | معرف الـ Admin الذي راجع الطلب |
| `reviewed_at` | TIMESTAMPTZ | تاريخ المراجعة |
| `admin_notes` | TEXT | ملاحظات الإدارة |

---

## 🎨 المميزات:

- ✅ واجهة جميلة مع Dark Mode
- ✅ رسائل واضحة للمستخدم
- ✅ Modal احترافي لطلب الحذف
- ✅ إشعارات ملونة حسب الحالة
- ✅ منع طلبات متعددة (يظهر حالة الطلب الحالي فقط)

---

## 🚀 الخطوات التالية:

1. ✅ شغل `account_deletion_requests_table.sql` في Supabase
2. ✅ جرب طلب حذف حساب كعميل
3. ⏳ أنشئ صفحة Admin لإدارة الطلبات (لاحقاً)

---

**النظام جاهز للاستخدام! 🎉**






