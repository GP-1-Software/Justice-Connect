# 👨‍💼 صفحة إدارة طلبات حذف الحسابات - Admin Dashboard

## ✅ ما تم إنشاؤه:

### **صفحة Admin لإدارة طلبات حذف الحسابات**
- ✅ عرض جميع طلبات الحذف المعلقة
- ✅ تفاصيل كل طلب (اسم، إيميل، سبب، تاريخ)
- ✅ إمكانية الموافقة أو الرفض
- ✅ إضافة ملاحظات من الإدارة
- ✅ دعم Dark Mode كامل
- ✅ تصميم احترافي وسهل الاستخدام

---

## 🚀 كيفية الوصول للصفحة:

### **الطريقة 1: عبر الرابط المباشر**
```
http://localhost:5173/admin/deletion-requests
```

### **الطريقة 2: إضافة زر في Admin Dashboard**
يمكنك إضافة زر في صفحة `AdminDashboard.jsx`:

```jsx
<Link to="/admin/deletion-requests">
  <button className="px-4 py-2 bg-red-600 text-white rounded-md">
    طلبات حذف الحسابات
  </button>
</Link>
```

---

## 📋 كيف تعمل الصفحة:

### **1. عرض الطلبات:**
- تعرض جميع طلبات الحذف المعلقة (`status = 'pending'`)
- كل طلب يعرض:
  - ✅ اسم المستخدم
  - ✅ الإيميل والهاتف
  - ✅ سبب طلب الحذف
  - ✅ تاريخ الطلب
  - ✅ نوع المستخدم (عميل/محامي)

### **2. مراجعة الطلب:**
- اضغط على زر **"مراجعة"**
- سيظهر Modal مع حقل لكتابة الملاحظات
- اختر:
  - **موافقة** = سيتم حذف الحساب تلقائياً
  - **رفض** = سيتم رفض الطلب مع إرسال الملاحظات للمستخدم

### **3. الموافقة على الحذف:**
- عند الموافقة، الحساب يُحذف تلقائياً من قاعدة البيانات
- يتم تحديث حالة الطلب إلى `approved`
- يظهر للمستخدم أن طلبه تمت الموافقة عليه

### **4. رفض الطلب:**
- يجب كتابة سبب الرفض (مطلوب)
- يتم تحديث حالة الطلب إلى `rejected`
- يظهر للمستخدم سبب الرفض

---

## 🎨 المميزات:

### **واجهة المستخدم:**
- ✅ تصميم نظيف واحترافي
- ✅ دعم Dark Mode
- ✅ رسائل نجاح وخطأ واضحة
- ✅ أيقونات ملونة حسب الحالة
- ✅ Modal جميل للمراجعة
- ✅ Loading states

### **الأمان:**
- ✅ Admin فقط يمكنه الوصول للصفحة
- ✅ كل طلب يجب مراجعته
- ✅ الحساب لا يُحذف إلا بعد موافقة Admin

---

## 🔧 الإعدادات المطلوبة:

### **1. Admin Authentication:**
الصفحة حالياً تحصل على `admin_id` من `localStorage`:

```javascript
const adminData = JSON.parse(localStorage.getItem('admin') || '{}');
const adminId = adminData.admin_id || 1;
```

**يجب عليك:**
- إضافة نظام تسجيل دخول للـ Admin
- حفظ بيانات Admin في `localStorage` أو Context
- إضافة حماية للصفحة (Protected Route)

### **2. Protected Route:**
أضف حماية للصفحة في `App.jsx`:

```jsx
import { Navigate } from 'react-router-dom';

// في App.jsx
<Route 
  path="/admin/deletion-requests" 
  element={
    isAdmin ? <DeletionRequests /> : <Navigate to="/admin/login" />
  } 
/>
```

---

## 📊 البيانات المعروضة:

### **كل طلب يعرض:**
- 🆔 معرف الطلب
- 👤 اسم المستخدم الكامل
- 📧 البريد الإلكتروني
- 📱 رقم الهاتف
- 💬 سبب طلب الحذف
- 📅 تاريخ الطلب
- 🏷️ نوع المستخدم (عميل/محامي)

---

## 🧪 الاختبار:

### **1. قم بإنشاء طلب حذف:**
- سجل دخول كعميل
- اذهب إلى الملف الشخصي → إدارة الحساب
- اطلب حذف الحساب مع كتابة سبب

### **2. راجع الطلب كـ Admin:**
- اذهب إلى `/admin/deletion-requests`
- ستظهر قائمة بجميع الطلبات المعلقة
- اضغط "مراجعة" على أي طلب
- اختر الموافقة أو الرفض

### **3. تحقق من النتيجة:**
- اذهب لحساب العميل
- ستظهر حالة الطلب (موافق/مرفوض)

---

## 🎯 الخطوات التالية:

1. ✅ شغل `account_deletion_requests_table.sql` في Supabase
2. ✅ جرب إنشاء طلب حذف كعميل
3. ✅ اذهب إلى `/admin/deletion-requests` كـ Admin
4. ✅ راجع الطلب واختر الموافقة أو الرفض
5. ⏳ أضف حماية للصفحة (Admin Authentication)
6. ⏳ أضف زر في Admin Dashboard للوصول السريع

---

## 📝 ملاحظات:

- الصفحة تعمل حالياً بدون حماية (يمكن لأي شخص الوصول لها)
- يجب إضافة نظام تسجيل دخول للـ Admin
- يجب حفظ `admin_id` في `localStorage` أو Context
- يمكنك إضافة فلترة للطلبات (حسب التاريخ، النوع، إلخ)

---

**الصفحة جاهزة للاستخدام! 🎉**






