=====================================================
NEW TABLES ADDED - Admin Enhancements
تاريخ الإضافة: November 2024
=====================================================

admin_activity_logs:
log_id,uuid,PK,,NOT NULL
,admin_id,integer,,FK → admins(admin_id),NOT NULL
,admin_name,character varying,,,NOT NULL
,action_type,character varying,,,NOT NULL
,target_type,character varying,,,NOT NULL
,target_id,character varying,,,NULL
,target_name,character varying,,,NULL
,description,text,,,NULL
,metadata,jsonb,,,NULL
,ip_address,character varying,,,NULL
,user_agent,text,,,NULL
,created_at,timestamp with time zone,,,NOT NULL

system_settings:
setting_id,uuid,PK,,NOT NULL
,setting_key,character varying,,,NOT NULL (UNIQUE)
,setting_value,jsonb,,,NOT NULL
,setting_category,character varying,,,NOT NULL
,description,text,,,NULL
,updated_by,integer,,FK → admins(admin_id),NULL
,updated_at,timestamp with time zone,,,NULL
,created_at,timestamp with time zone,,,NULL

admin_notifications:
notification_id,uuid,PK,,NOT NULL
,admin_id,integer,,FK → admins(admin_id),NULL
,title,character varying,,,NOT NULL
,message,text,,,NOT NULL
,type,character varying,,,NOT NULL
,priority,character varying,,,NULL
,related_type,character varying,,,NULL
,related_id,character varying,,,NULL
,action_url,text,,,NULL
,is_read,boolean,,,NULL
,read_at,timestamp with time zone,,,NULL
,created_at,timestamp with time zone,,,NULL

=====================================================
UPDATED TABLES - New Columns
=====================================================

cases (NEW COLUMNS):
,is_disabled,boolean,,,NULL (DEFAULT FALSE)
,disabled_at,timestamp with time zone,,,NULL
,disabled_by,integer,,FK → admins(admin_id),NULL
,disabled_reason,text,,,NULL
,disabled_admin_notes,text,,,NULL

=====================================================
ENUMS & CONSTRAINTS
=====================================================

action_type (admin_activity_logs):
- approve_user
- reject_user
- approve_lawyer
- reject_lawyer
- disable_case
- enable_case
- delete_account
- update_settings
- promote_to_admin
- demote_admin

target_type (admin_activity_logs):
- user
- lawyer
- case
- appointment
- payment
- invoice
- system
- admin

notification_type (admin_notifications):
- info
- warning
- error
- success
- urgent

notification_priority (admin_notifications):
- low
- normal
- high
- urgent

setting_category (system_settings):
- general
- payment
- email
- security
- notifications

=====================================================
INDEXES
=====================================================

admin_activity_logs:
- idx_activity_logs_admin_id ON (admin_id)
- idx_activity_logs_action_type ON (action_type)
- idx_activity_logs_target_type ON (target_type)
- idx_activity_logs_created_at ON (created_at DESC)
- idx_activity_logs_target_id ON (target_id)

admin_notifications:
- idx_admin_notifications_admin_id ON (admin_id)
- idx_admin_notifications_is_read ON (is_read)
- idx_admin_notifications_created_at ON (created_at DESC)
- idx_admin_notifications_priority ON (priority)

cases:
- idx_cases_is_disabled ON (is_disabled)

=====================================================
FUNCTIONS
=====================================================

log_admin_activity(
    p_admin_id INTEGER,
    p_admin_name VARCHAR,
    p_action_type VARCHAR,
    p_target_type VARCHAR,
    p_target_id VARCHAR,
    p_target_name VARCHAR,
    p_description TEXT,
    p_metadata JSONB DEFAULT NULL
) RETURNS UUID

get_admin_dashboard_stats() RETURNS JSONB

notify_case_disabled() RETURNS TRIGGER

generate_random_activities(
    p_admin_id INTEGER,
    p_admin_name VARCHAR,
    p_count INTEGER DEFAULT 20
) RETURNS void

cleanup_test_data() RETURNS void

=====================================================
TRIGGERS
=====================================================

trigger_notify_case_disabled
- ON: cases
- WHEN: UPDATE (is_disabled changes)
- EXECUTE: notify_case_disabled()

=====================================================
RLS POLICIES
=====================================================

admin_activity_logs:
- "Admins can view all activity logs" (SELECT)
  → Only authenticated admins

system_settings:
- "Admins can view system settings" (SELECT)
  → Only authenticated admins
- "Super admins can update system settings" (UPDATE)
  → Only super_admin role

admin_notifications:
- "Admins can view their own notifications" (SELECT)
  → Only own notifications
- "Admins can update their own notifications" (UPDATE)
  → Only own notifications

=====================================================
DEFAULT SETTINGS (system_settings)
=====================================================

platform_commission_rate:
{"rate": 10, "type": "percentage"}

min_appointment_price:
{"amount": 50, "currency": "JOD"}

max_appointment_duration:
{"minutes": 180}

email_notifications_enabled:
{"enabled": true}

maintenance_mode:
{"enabled": false, "message": ""}

=====================================================
VIEWS
=====================================================

recent_admin_activities:
- Combines admin_activity_logs with admins
- Shows last 100 activities
- Includes admin role
- Ordered by created_at DESC

=====================================================
NOTES
=====================================================

1. All new tables have RLS enabled
2. UUID primary keys for better scalability
3. JSONB for flexible metadata storage
4. Comprehensive indexing for performance
5. Automatic triggers for notifications
6. Audit trail for all admin actions

=====================================================
